<!-- pages/index/index.wxml ���մ� -->
<view class="page">
  <!-- �������� -->
  <view class="header">
    <view class="h-date">{{todayDate}}</view>
    <view class="h-week">{{weekdayText}}</view>
  </view>

  <!-- ���� -->
  <view class="overview card">
    <view class="ov-item">
      <view class="ov-num">{{doneCount}}<text class="ov-unit">/3</text></view>
      <view class="ov-label">����ѧϰ</view>
    </view>
    <view class="ov-divider"></view>
    <view class="ov-item">
      <view class="ov-num">{{streak}}</view>
      <view class="ov-label">��������</view>
    </view>
    <view class="ov-stats" bindtap="goStats">�9�6 ͳ��</view>
  </view>

  <!-- ѧϰ��� -->
  <view class="sec-title">�9�8 ѧϰ��</view>
  <view class="card study {{item.done ? 'study-done' : ''}}"
        wx:for="{{sections}}" wx:key="key"
        bindtap="onStudyTap" data-key="{{item.key}}">
    <view class="study-emoji">{{item.emoji}}</view>
    <view class="study-mid">
      <view class="study-name">{{item.name}}</view>
      <view class="study-sub" wx:if="{{item.done}}">�Ѵ� �� ��ʱ {{item.duration}} ��</view>
      <view class="study-sub muted" wx:else>δ��ɣ������</view>
    </view>
    <view class="study-flag">{{item.done ? '�7�3' : '��'}}</view>
  </view>

  <!-- �ճ����� -->
  <view class="sec-title">�9�1�1�5 �ճ�����</view>
  <view class="card fitness" bindtap="openFitness">
    <block wx:if="{{todayFitness}}">
      <view class="fit-type">{{todayFitness.typeText}}</view>
      <view class="fit-sub">ʱ�� {{todayFitness.duration}} ��
        <text wx:if="{{todayFitness.distance}}"> �� {{todayFitness.distance}} km</text>
        <text wx:if="{{todayFitness.weight}}"> �� {{todayFitness.weight}} kg</text>
      </view>
    </block>
    <view class="fit-empty" wx:else>�����¼���ս��� ��</view>
  </view>

  <!-- �ײ������� -->
  <view class="quick" bindtap="goWrongbook">�� �Ӵ���</view>

  <!-- ������¼���� -->
  <view class="mask" wx:if="{{showFitness}}" bindtap="closeFitness"></view>
  <view class="sheet" wx:if="{{showFitness}}">
    <view class="sheet-title">��¼����</view>

    <view class="field">
      <text class="field-label">����</text>
      <picker class="field-picker" range="{{typeOptions}}" value="{{typeIndex}}" bindchange="onTypeChange">
        <view class="picker-text">{{fitnessForm.typeText}} �7�0</view>
      </picker>
    </view>

    <view class="field">
      <text class="field-label">ʱ��(��)</text>
      <input class="field-input" type="number" placeholder="30-40" value="{{fitnessForm.duration}}" data-field="duration" bindinput="onFitnessInput"/>
    </view>

    <view class="field" wx:if="{{fitnessForm.type === 'run'}}">
      <text class="field-label">����(km)</text>
      <input class="field-input" type="digit" placeholder="�� 6.0" value="{{fitnessForm.distance}}" data-field="distance" bindinput="onFitnessInput"/>
    </view>

    <view class="field">
      <text class="field-label">����(kg)</text>
      <input class="field-input" type="digit" placeholder="��ѡ" value="{{fitnessForm.weight}}" data-field="weight" bindinput="onFitnessInput"/>
    </view>

    <view class="field">
      <text class="field-label">ϥ����ʹ(0-10)</text>
      <input class="field-input" type="number" placeholder="0" value="{{fitnessForm.knee}}" data-field="knee" bindinput="onFitnessInput"/>
    </view>

    <view class="field">
      <text class="field-label">�N����ʹ(0-10)</text>
      <input class="field-input" type="number" placeholder="0" value="{{fitnessForm.popliteal}}" data-field="popliteal" bindinput="onFitnessInput"/>
    </view>

    <view class="field">
      <text class="field-label">��ע</text>
      <input class="field-input" placeholder="��ѡ" value="{{fitnessForm.note}}" data-field="note" bindinput="onFitnessInput"/>
    </view>

    <view class="sheet-btns">
      <button class="btn-cancel" bindtap="closeFitness">ȡ��</button>
      <button class="btn-save" bindtap="saveFitness">����</button>
    </view>
  </view>
</view>
