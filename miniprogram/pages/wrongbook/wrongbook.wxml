<!-- pages/wrongbook/wrongbook.wxml ���Ȿ -->
<view class="page">
  <!-- ����ɸѡ -->
  <view class="filters">
    <view wx:for="{{filters}}" wx:key="key"
          class="filter {{activeFilter === item.key ? 'active' : ''}}"
          bindtap="onFilter" data-key="{{item.key}}">{{item.label}}</view>
  </view>

  <!-- �б� -->
  <view wx:if="{{list.length === 0}}" class="empty-tip">���޴��⣬������Ͻ� + ����</view>
  <view class="card item" wx:for="{{list}}" wx:key="_id">
    <view class="item-top">
      <text class="tag tag-{{item.prefix}}">{{item.prefixName}}</text>
      <view class="item-ops">
        <text class="op" bindtap="openEdit" data-id="{{item._id}}">�༭</text>
        <text class="op op-del" bindtap="deleteWrong" data-id="{{item._id}}">ɾ��</text>
      </view>
    </view>
    <view class="q">Q��{{item.question}}</view>
    <view class="a" wx:if="{{item.myAnswer}}">�ҵĴ𰸣�{{item.myAnswer}}</view>
    <view class="r" wx:if="{{item.wrongReason}}">����{{item.wrongReason}}</view>
  </view>

  <!-- ������ť -->
  <view class="add-btn" bindtap="openAdd">�� ���Ӵ���</view>

  <!-- �༭���� -->
  <view class="mask" wx:if="{{showEditor}}" bindtap="closeEditor"></view>
  <view class="sheet" wx:if="{{showEditor}}">
    <view class="sheet-title">{{editing ? '�༭����' : '��������'}}</view>

    <view class="field">
      <text class="field-label">����</text>
      <picker class="field-picker" range="{{prefixOptions}}" value="{{prefixIndex}}" bindchange="onPrefixChange">
        <view class="picker-text">{{form.prefix}} �7�0</view>
      </picker>
    </view>
    <view class="field-col">
      <text class="field-label">��Ŀ</text>
      <textarea class="field-area" placeholder="������Ŀ" value="{{form.question}}" data-field="question" bindinput="onFormInput"/>
    </view>
    <view class="field-col">
      <text class="field-label">�ҵĴ�</text>
      <textarea class="field-area" placeholder="��ѡ" value="{{form.myAnswer}}" data-field="myAnswer" bindinput="onFormInput"/>
    </view>
    <view class="field-col">
      <text class="field-label">����</text>
      <textarea class="field-area" placeholder="��ѡ" value="{{form.wrongReason}}" data-field="wrongReason" bindinput="onFormInput"/>
    </view>

    <view class="sheet-btns">
      <button class="btn-cancel" bindtap="closeEditor">ȡ��</button>
      <button class="btn-save" bindtap="saveWrong">����</button>
    </view>
  </view>
</view>
